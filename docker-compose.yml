version: '3.1'

services:

  api:
    container_name: api
    build: .
    ports:
      - 8080:8080
    depends_on:
      - db
    environment:
      DATABSE_URL: jdbc:mysql://db:3306/moviecatalog
      DATABSE_USERNAME: root
      DATABSE_PASSWORD: iuGIuy65dai7a6dagfd
      SPRING_DATASOURCE_URL: jdbc:mysql://db:3306/moviecatalog?useUnicode=true&characterEncoding=utf8&useSSL=false

  db:
    container_name: db
    image: mariadb
    restart: always
    ports:
      - 3306:3306
    environment:
      MYSQL_ROOT_PASSWORD: iuGIuy65dai7a6dagfd

  adminer:
    container_name: adminer
    image: adminer
    restart: always
    ports:
      - 8081:8080
    depends_on:
      - db